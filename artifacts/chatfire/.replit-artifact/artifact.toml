kind = "web"
previewPath = "/"
title = "ChatFire Ultra Edition"
version = "1.0.0"
id = "artifacts/chatfire"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21838

[services.development]
run = "pnpm --filter @workspace/chatfire run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/chatfire", "run", "build" ]
publicDir = "artifacts/chatfire/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21838"
BASE_PATH = "/"
